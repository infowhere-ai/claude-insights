# Session Log — 2026-03-30

> Projeto: claude-monitor
> Perfil: private

## Resumo do Dia
(será completado no /finalizar-dia)

## Atividades Detalhadas

### 1. Events log, context stats, hidden files filter, badge labels
**O que foi feito**:
- Adicionado log de eventos SSE na tab Events
- Adicionadas estatísticas de contexto (tokens usados)
- Adicionado filtro de ficheiros ocultos (`.hidden`) na tab Context
- Adicionados badge labels nas tabs com contadores

**Arquivos modificados**:
- `static/index.html` — múltiplas features frontend

**Resultado**: Tab Events e Context com mais informação útil

---

### 2. Pending projects notification bell
**O que foi feito**:
- Adicionado botão de notificação (bell) na toolbar para projetos pendentes
- Pendentes = projetos com `.claude/` mas sem `status.json` ativo

**Arquivos modificados**:
- `static/index.html` — bell button + pending overlay modal

**Resultado**: Utilizador consegue ver projetos descobertos mas não ativos

---

### 3. Refactor sidebar — remoção do modal "Projects to monitor"
**O que foi feito**:
- Removido modal `proj-filter-overlay` e botão da toolbar
- Removido botão de bell de pendentes e overlay associado
- Sidebar passa a mostrar apenas projetos ativos (com sessão Claude ativa)
- Raízes de monitorização movidas para tab Setup
- Corrigido flood de 20+ projetos pendentes no sidebar (todos os repos de `github-infowhere` têm `.claude/`)

**Arquivos modificados**:
- `static/index.html` — ~250 linhas CSS removidas, `renderAll()` reescrito, funções órfãs removidas

**Resultado**: Sidebar limpo, sem modal, sem flood de pendentes

---

### 4. IntelliJ-style project picker panel
**O que foi feito**:
- Adicionado painel lateral colapsável de 200px dentro do `.left-panel`
- Botão toggle (⊞) na toolbar + botão "Browse all projects" com destaque índigo na base do sidebar
- O painel lista todos os projetos descobertos: ativos (dot verde) e pendentes (dot cinza)
- Utilizador pode "pinnear" qualquer projeto para o manter visível no sidebar mesmo sem sessão ativa
- `pinnedProjects` (Set) persistido em `localStorage` sob chave `cm-pinned-projects`
- Projetos pinados aparecem como placeholder cards no sidebar com botão de unpin

**Arquivos modificados**:
- `static/index.html` — CSS picker, HTML picker strip, JS: `pinnedProjects`, `loadPinnedProjects()`, `savePinnedProjects()`, `togglePinned()`, `togglePicker()`, `renderPicker()`, `renderAll()` atualizado

**Resultado**: Utilizador pode selecionar/pinnear qualquer projeto descoberto para o sidebar, sem modal

---

### 5. Remoção de "Monitored Folders" do Setup tab
**O que foi feito**:
- Secção "Monitored Folders" removida do tab Setup a pedido do utilizador
- HTML e chamada a `renderRootsList()` removidos de `_renderSetupTab()`

**Arquivos modificados**:
- `static/index.html` — bloco HTML da secção e chamada JS removidos

**Resultado**: Tab Setup sem a secção de pastas monitorizadas

---

## Commits do Dia

| Repositorio | Hash | Mensagem |
|-------------|------|----------|
| claude-monitor | `782dd71` | feat(app,frontend): events log, context stats, hidden files filter, badge labels |
| claude-monitor | `47b3784` | feat(app,frontend): pending projects notification bell in toolbar |
| claude-monitor | `a140176` | feat(frontend): unified sidebar — remove pending flood, move roots to setup tab |
| claude-monitor | `783b01c` | feat(frontend): add IntelliJ-style project picker panel |
| claude-monitor | `bb96c86` | fix(frontend): improve project picker visibility, remove monitored folders from setup |
