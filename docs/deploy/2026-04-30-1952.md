# Deploy Report — claude-monitor

> Data: 2026-04-30 19:52
> Commit: `507623c` — chore(merge): merge develop into main
> Branch: main
> Run ID: 25186152351
> Workflow: release

## Resumo

| Métrica | Valor |
|---------|-------|
| Resultado | **SUCCESS** (após 2 re-runs) |
| Jobs executados | 8 |
| Re-runs | 2 |
| Deploy Site | SUCCESS (Cloudflare Pages) |

## Jobs

| Job | Status | Notas |
|-----|--------|-------|
| Build macOS binary (arm64) | ✅ success | |
| Build Linux binary (x86_64) | ✅ success | |
| Build Python wheel | ✅ success | |
| Smoke test artefacts | ✅ success | |
| Create GitHub Release | ✅ success | v1.0.0b1 criado em infowhere-ai/claude-insights |
| Build and attach .deb | ✅ success | |
| Update Homebrew formula | ✅ success | Após fix do HOMEBREW_TAP_TOKEN |
| Publish to PyPI | ✅ success | v1.0.0b1 já estava publicado (skip_existing) |
| Deploy Site | ✅ success | Cloudflare Pages — monitor.infowhere.ai |

## Falhas e Resolução

| Falha | Causa | Resolução |
|-------|-------|-----------|
| Homebrew 403 | `HOMEBREW_TAP_TOKEN` era token da conta `infowhere-be`, sem acesso a `infowhere-ai/homebrew-claude-insights` | Substituído por token da conta `infowhere-ai` |
| PyPI `PYPI_TOKEN` não configurado | Secret em falta no repo | Adicionado token de `op://INFOWHERE-AI/PYPI-TOKEN/API_TOKEN` |
| PyPI `File already exists` | `1.0.0b1` já publicado na primeira run — PyPI não permite re-upload | Adicionado `skip_existing: true` ao workflow |

## Notas

- Repo público `infowhere-ai/claude-insights` foi recriado com história limpa (sem ficheiros privados)
- Standards `opensource-release.md` criados em `standarts/common/rules/`
- Landing page `monitor.infowhere.ai` configurada via Cloudflare Pages (Direct Upload via Wrangler + deploy automático em push para main quando `site/**` muda)
